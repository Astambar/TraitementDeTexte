#include "traitement_de_texte.h"

EmailInfo isEmailAtPosition(const char* text, int position, const char* separators)
{
	// TODO: Identifier tout type d'email

	EmailInfo emailInfo = {NULL, position};
	return emailInfo;
}